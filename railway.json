{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev && php artisan key:generate --force && php artisan storage:link && php artisan optimize"
  },
  "deploy": {
    "preDeployCommand": "php artisan migrate --force && php artisan optimize:clear && php artisan view:clear && php artisan config:clear && php artisan route:clear && php artisan view:cache && php artisan config:cache && php artisan route:cache"
  },
  "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
}
